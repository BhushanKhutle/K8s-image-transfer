# Backend Configuration
NODE_ENV=production
PORT=3001
DB_PATH=/app/data/k8s-transfer.db

# SSH Configuration
SSH_TIMEOUT=30000
TRANSFER_TIMEOUT=300000

# CORS
CORS_ORIGIN=http://localhost:3000

# Kubeconfig path (for kubectl discovery)
# KUBECONFIG=/root/.kube/config
